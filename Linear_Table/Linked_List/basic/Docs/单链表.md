一.结点类型的定义
typedef struct Node {
    ElemType data;
    struct Node *next;
} Node, *LinkList;

等价于:

typedef strcuct Node {
    ElemType data;
    strcut Node* next;
} Node;
typedef struct Node* LinkList;s



